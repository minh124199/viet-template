package io.github.minh124199.viettemplate.tck.velocity.result;

/** Normalized semantic categories for exceptions thrown across Velocity and Viet Template. */
public enum ExceptionCategory {
  SYNTAX_ERROR,
  UNDEFINED_REFERENCE,
  INVALID_PROPERTY,
  INVALID_METHOD,
  SECURITY_DENIED,
  RESOURCE_NOT_FOUND,
  TYPE_ERROR,
  ARITHMETIC_ERROR,
  LIMIT_EXCEEDED,
  OTHER_RENDER_ERROR
}
