# Viet Template 1.0 Readiness Gap Analysis & Surface Audit

This document provides an exhaustive gap analysis evaluating Viet Template against the **Definition of Done for 1.0.0 General Availability (GA)**. It audits all fifteen architectural areas, details the 121 stable public types (94 API + 27 SPI) versus 85 non-blocking P2 debt types across 5 baselines, outlines the encapsulation remediation roadmap, and provides an authoritative release recommendation.

---

## 1. Executive Summary & 1.0 Readiness Verdict

> [!IMPORTANT]
> **Verdict: READY_FOR_1_0_API_FREEZE_AND_RC_PREPARATION**
>
> Viet Template has satisfied all criteria for 1.0 candidate release across all fifteen architectural and operational dimensions with **0 P0/P1 blockers**.
>
> Following comprehensive internal surface encapsulation across milestones 0.3.0-M1 through 0.3.0-M3.5 and formal contract evidence reconciliation in milestone 0.3.0-M7.9:
> - **Public API/SPI Surface**: Exactly 121 stable types (94 `STABLE_API` + 27 `STABLE_SPI`) across 5 baselines, 5 `EXPERIMENTAL` types, and 339 total compiled public types across 11 modules.
> - **Internal Surface Containment**: 85 non-blocking P2 PBCIA debt types (41 AST, 33 IR, 11 Semantics) tracked in `config/architecture/public-surface-debt-registry.json` with **0 signature leaks** into public API/SPI, zero public reachability, and structural module realignment deferred to M22.
> - **Generated Runtime ABI**: Exactly 7 types and 22 methods (0 fields, 0 unregistered dependencies) established in 1.0 candidate baseline and verified under `scripts/verify-generated-abi.py` (READY_TO_FREEZE).
> - **Diagnostic Codes**: Exactly 31 canonical codes established in 1.0 candidate baseline (`config/api-baseline/diagnostic-codes-1.0.txt`) and verified under `scripts/verify-diagnostic-codes.py` (READY_TO_FREEZE).
> - **Release Infrastructure**: 12 production modules + parent POM (13 published artifacts) verified under `config/compatibility/publication-topology.json`, 2 non-published verification modules, and factual manifest audit confirming absence of `Automatic-Module-Name`.
> - **Candidate Contract**: Authoritative aggregator verification (`scripts/verify-1.0-readiness.py`) validates candidate contract compliance and outputs verdict `READY_FOR_1_0_API_FREEZE_AND_RC_PREPARATION`.

### Recommended Release Sequence
1. **0.2.2 (Historical Latest Published Release)**:
   - Published on Maven Central (2026-09-20); version 0.2.3 was prepared across POMs and build scripts (commit `b8b3a8f`) but held without tagging or publication (`PREPARED_HELD`).
2. **0.3.0 Encapsulation & M7.9 Reconciliation (COMPLETE)**:
   - Completed package-private visibility reductions (M1), `*.internal.*` package moves (M2), public surface taxonomy and entrypoint containment (M3.3), internal contract formalization (M3.4), architecture convergence and debt registry (M3.5), and M7.9.1–M7.9.6 contract evidence baselines and verifiers.
3. **1.0.0-RC1 (Release Candidate & API Freeze)**:
   - Freeze the 121 stable API/SPI types, 7 generated ABI types (22 methods), and 31 diagnostic codes. Initiate community validation and release candidate packaging.
4. **1.0.0 GA (General Availability)**:
   - Lock permanent binary and source backwards compatibility under Semantic Versioning 2.0.0.

---

## 2. Fifteen-Point 1.0 Readiness Audit Table

| # | Area | Status | Evidence | Missing Work for 1.0 GA | 1.0 Blocker? |
|---|---|---|---|---|---|
| **1** | **Language Compatibility** | **COMPLETE** | 80/80 features passing in TCK; 76 exact matches with Apache Velocity 2.4.1, 3 documented intentional differences, 1 extension. `docs/migration/compatibility-matrix.md`. | None. | **No** |
| **2** | **Runtime Interpreter** | **COMPLETE** | `VtlInterpreter` with array-backed `ExecutionFrame` slots, 3-state evaluation (`UNDEFINED`, `DEFINED_NULL`, `DEFINED_VALUE`), monomorphic and polymorphic inline caches (PIC). | None. | **No** |
| **3** | **Bytecode Compiler & AOT** | **COMPLETE** | Bytecode template compiler, `templates.idx` binary indexing, `rejectRuntimeCompilation` security lock, zero-reflection execution. `docs/deployment/production-aot.md`. | None. | **No** |
| **4** | **Spring Boot Integration** | **COMPLETE** | Auto-configuration for Spring Boot 4 / Framework 7, property catalog (`viet-template.*`), WebMvc and WebFlux reactive view resolvers. `docs/spring/spring-boot-integration.md`. | None. | **No** |
| **5** | **Spring Security Integration** | **COMPLETE** | Contextual HTML escaping, strict Content Security Policy (CSP) nonces, secure attribute wrapping. `docs/security/secure-templates.md`. | None. | **No** |
| **6** | **Native Image (GraalVM) & AOT** | **COMPLETE** | Quarkus native image binary builds empirically qualified on Linux x86_64 Mandrel 25.0.4.1-Final (Java 25). Spring AOT qualified as `JVM_AOT_QUALIFIED` (Spring AOT code generation, resource/reflection hints, and packaged executable JAR verified under `scripts/verify-spring-integration-parity.sh`); Spring native-image binary compilation remains separate. | None for 1.0 candidate. | **No** |
| **7** | **Build Tooling** | **COMPLETE** | Maven Plugin (`io.github.minh124199:viet-template-maven-plugin`) and Gradle Plugin (`io.github.minh124199.viet-template`) with full AOT precompilation support. `docs/build-tooling/`. | None. | **No** |
| **8** | **Performance & Benchmarks** | **COMPLETE** | Workloads C01–C08 qualified across Java 21 and Java 25. All 4 performance gates passed. DSA stopping rule satisfied under M19.3c. `docs/performance/comparative-benchmarks.md`. | None (runtime optimizations frozen). | **No** |
| **9** | **Security & Sandboxing** | **COMPLETE** | `MemberAccessPolicy` (standard, strict, permissive), `RenderBudget` (CPU time, iteration, memory limits), template path confinement. `docs/security/secure-templates.md`. | None. | **No** |
| **10** | **Diagnostics & Error Catalog** | **COMPLETE** | Precise 1-based line and column tracking, qualified diagnostic codes (`SYNTAX:*`, `VTLS:*`, `SECURITY:*`, `LIMIT:*`), resolution remedies. `docs/diagnostics/error-catalog.md`. | None. | **No** |
| **11** | **Public API & Surface Boundary** | **COMPLETE (0 P0/P1 blockers; 85 P2 non-blocking debt types tracked in registry)** | 121 types verified as `STABLE_API` (94) or `STABLE_SPI` (27); 339 compiled types across 11 active categories (`FRAMEWORK_ENTRYPOINT`: 4, `BUILD_TOOL_ENTRYPOINT`: 4, `GENERATED_RUNTIME_ABI`: 1, `INTERNAL_CROSS_MODULE`: 59 [56 production], `INTERNAL_CROSS_PACKAGE`: 57, `BENCHMARK_SUPPORT_INTERNAL`: 3, `PUBLIC_BUT_INTERNAL_ACCIDENT`: 85). Tracked in `config/architecture/public-surface-debt-registry.json`. ADR-0017. | None for 1.0 candidate freeze (85 P2 PBCIA debt types deferred to M22 module boundary review). | **No** |
| **12** | **Documentation Suite** | **COMPLETE** | Complete user documentation suite across 11 dedicated topics, verified continuously by `scripts/verify-documentation.py`. | Real-world adopter case studies and tutorials. | **No** |
| **13** | **Dual-Build Parity** | **COMPLETE** | Both Maven (`./mvnw`) and Gradle (`./gradlew`) build all modules, execute tests, and package identical artifacts. Verified by `scripts/verify-build-parity.py`. | None. | **No** |
| **14** | **Testing & Qualification** | **COMPLETE** | Exhaustive unit tests, integration tests, concurrency stress suites (Virtual Threads), mutation tests, and spotless style checks. | External user feedback and community battle-testing. | **No** |
| **15** | **Release Infrastructure & Publication Scope** | **COMPLETE** | Automated Maven Central publishing and GitHub Releases verified with `v0.2.2`. Cryptographic signatures and release metadata checks active. Publication taxonomy defined in `config/compatibility/publication-topology.json`: 12 production modules + parent POM intended for 1.0; 2 internal verification modules (`viet-template-tck`, `viet-template-benchmarks`) strictly non-published. Historical status of `0.2.3` reconciled as `PREPARED_HELD` (commit `b8b3a8f`, never tagged/published); `0.3.0-SNAPSHOT` strictly unpublished. Manifest audit confirms factual absence of `Automatic-Module-Name` (`NO_AUTOMATIC_MODULE_NAME_HEADER`); JPMS deferred to post-1.0 (M22). Gradle plugin published to Maven Central only. | None. | **No** |

---

## 3. Surface Audit: 121 Stable Types vs 339 Total Compiled Types (Post-0.3.0-M6.9)

A comprehensive audit of `config/api-baseline/public-surface-classification.txt` and the 5 public API baselines reveals the composition of exported Java types across all 11 published modules with Java code. As of milestone **0.3.0-M6.9**, the public surface taxonomy is fully established and mechanically enforced:

| Category | Count | Description |
|---|---|---|
| `STABLE_API` | 94 | Consumer-facing contracts under long-term 1.x backward compatibility guarantee |
| `STABLE_SPI` | 27 | Extension/contributor interfaces for third-party implementors under 1.x guarantee (includes `FreshnessToken`, `TemplateFreshnessProvider`) |
| `EXPERIMENTAL` | 5 | Emerging language/compiler APIs subject to evolution before 1.0 |
| `FRAMEWORK_ENTRYPOINT` | 4 | Classes instantiated by Spring AOT, Quarkus Arc/CDI or build steps |
| `BUILD_TOOL_ENTRYPOINT` | 4 | Classes instantiated by Maven or Gradle tooling |
| `SERVICE_ENTRYPOINT` | 0 | `ServiceLoader` provider classes |
| `GENERATED_RUNTIME_ABI` | 1 | Primary taxonomy category: types exclusively designed as runtime bridge for generated AOT template bytecode (`BytecodeRuntimeBridge`) |
| `INTERNAL_CROSS_MODULE` | 59 | Cross-module internal contracts (56 production edges + 3 test-scope contracts) |
| `INTERNAL_CROSS_PACKAGE` | 57 | Types intentionally public within their artifact due to cross-package callers in the same JAR |
| `BENCHMARK_SUPPORT_INTERNAL` | 3 | Types exposed solely for unpublished JMH benchmark suite slot-level measurement |
| `PUBLIC_BUT_INTERNAL_ACCIDENT` | 85 | Accidental public classes tracked in debt registry awaiting M22 module boundary review |
| **Total** | **339** | |

### 3.1 Stable Public Types (121 Types)
The 121 stable types (94 `STABLE_API` + 27 `STABLE_SPI`) represent the intended permanent API/SPI contract of Viet Template across 5 baseline definitions:
- **Core Baseline (`1.0-core-public-api.txt` — 98 types: 95 API + 3 SPI)**:
  - Core engine entry points: `TemplateEngine`, `Template`, `CompiledTemplate`, `TemplateId`, `TemplateSource`.
  - Repository & loading: `TemplateRepository`, `ClasspathTemplateRepository`, `FileTemplateRepository`, `StringTemplateRepository`, `InMemoryTemplateRepository`, `CompositeTemplateRepository`.
  - Context & Configuration: `RenderContext`, `RenderRequest`, `TemplateSuffixConfiguration`, `UndefinedReferencePolicy`.
  - Security API: `MemberAccessPolicy`, `RenderBudget`, `TemplateSecurityException`.
  - SPI: `RenderContextContributor`, `FreshnessToken`, `TemplateFreshnessProvider`.
- **AOT Baseline (`1.0-aot-public-api.txt` — 6 types: 3 API + 3 SPI)**:
  - API: `TemplateAotCompiler`, `AotCompilationResult`, `AotCompilerOptions`.
  - SPI: `AotSourceProvider`, `AotClassConsumer`, `AotDiagnosticListener`.
- **Spring Baseline (`1.0-spring-public-api.txt` — 8 types: 7 API + 1 SPI)**:
  - API: `VietTemplateView`, `VietTemplateViewResolver`, `VietTemplateProperties`, `VietTemplateAutoConfiguration`.
  - SPI: `VietTemplateEngineCustomizer`.
- **Spring Security Baseline (`1.0-spring-security-public-api.txt` — 5 types: 0 API + 5 SPI)**:
  - SPI: `SecurityView`, `CsrfView`, `SecurityViewFactory`, `CsrfViewFactory`, `SpringSecurityRenderContextContributor`.
- **Quarkus Baseline (`1.0-quarkus-public-api.txt` — 4 types: 3 API + 1 SPI)**:
  - API: `VietTemplateConfig`, `VietTemplateRenderer`, `QuarkusSecurityView`.
  - SPI: `QuarkusSecurityRenderContextContributor`.

### 3.2 Generated Runtime ABI Taxonomy vs Bytecode Dependency Set
To ensure architectural precision, the repository distinguishes:
- **Primary Taxonomy Category (`GENERATED_RUNTIME_ABI = 1`)**: Exclusively classifies `BytecodeRuntimeBridge`, whose sole architectural purpose is mediating between generated template classfiles and internal runtime mechanisms.
- **Generated Bytecode Runtime Dependency Set (7 Types)**: The complete set of Viet-owned runtime classes referenced from generated template classfiles as verified by `scripts/verify-generated-abi.py`:
  1. `io.github.minh124199.viettemplate.api.CompiledTemplate` (`STABLE_API`)
  2. `io.github.minh124199.viettemplate.api.RenderContext` (`STABLE_API`)
  3. `io.github.minh124199.viettemplate.api.TemplateId` (`STABLE_API`)
  4. `io.github.minh124199.viettemplate.api.TemplateOutput` (`STABLE_API`)
  5. `io.github.minh124199.viettemplate.language.vtl.semantics.scope.ForeachMetadata` (`STABLE_API`)
  6. `io.github.minh124199.viettemplate.runtime.linker.DynamicCallSite` (`INTERNAL_CROSS_MODULE`)
  7. `io.github.minh124199.viettemplate.vtl.compiler.bytecode.BytecodeRuntimeBridge` (`GENERATED_RUNTIME_ABI`)

### 3.3 Accidental Public Types (Post-0.3.0-M3.5: 85 Types Remaining)
The original 259 types classified as `PUBLIC_BUT_INTERNAL_ACCIDENT` were mechanically audited and categorized in `config/api-baseline/accidental-public-types-inventory.json`. In milestone **0.3.0-M1**, 34 Category-A types were demoted to package-private. In **0.3.0-M2**, 49 Category-B types were relocated to `*.internal.*`. In **0.3.0-M3.3**, 56 Category-C1 types were reclassified to `INTERNAL_CROSS_MODULE`, 4 framework/build entrypoints were formally typed, 1 ABI type classified, and 1 C7 type (`ModelSchema$Builder`) was package-private contained. In **0.3.0-M3.4**, 6 interpreter contracts were formalized. In **0.3.0-M3.5**, 55 intra-module types were classified as `INTERNAL_CROSS_PACKAGE`, 3 benchmark-only types as `BENCHMARK_SUPPORT_INTERNAL`, and the remaining 85 types registered in `config/architecture/public-surface-debt-registry.json`:
1. **Category A — Package-Private (34 Types Demoted in 0.3.0-M1, 0 Remaining)**
2. **Category B — Internal Package (49 Types Relocated to `*.internal.*` in 0.3.0-M2, 0 Un-internalized Remaining)**
3. **Category C1 — INTERNAL_CROSS_MODULE (56 Production Contracts Verified in 0.3.0-M3.5)**:
   - 48 types from `language-vtl → vtl-interpreter` (AST, IR, Lexer, Semantics, Optimization).
   - 7 types from `runtime → vtl-interpreter` (linker internals: `AccessLink`, `CallSiteRegistry`, `DynamicCallSite$State`, `DynamicLinker`, `LinkerStatistics`, `MemberKey`, `MemberOperation`).
   - 1 type across `runtime → spring` and `runtime → quarkus` (`NonClosingOutputStream`).
4. **INTERNAL_CROSS_PACKAGE (55 Types Classified in 0.3.0-M3.5)**:
   - Deliberate intra-module package boundaries across `vtl.internal.compiler.*`, `vtl.internal.engine.*`, `vtl.internal.interpreter.*`, `vtl.engine.cache.*`, `vtl.engine.dependency.*`, `language.vtl.internal.*`, and `runtime.linker.*`. Not architectural debt.
5. **BENCHMARK_SUPPORT_INTERNAL (3 Types Classified in 0.3.0-M3.5)**:
   - `EvaluationValue$State`, `ExecutionFrame`, `ForeachMetadata` (vtl.interpreter) — consumed only by the unpublished JMH benchmark suite.
6. **Debt Registry (85 Types Remaining as PUBLIC_BUT_INTERNAL_ACCIDENT + 3 Non-Prod Contracts = 88 Total Debt Types)**:
   - AST concrete nodes (41 types): sealed hierarchy language topology, target M22 (`NON_BLOCKING_P2_FOR_1_0`). Candidate remedies: module consolidation, ownership restructuring, narrow internal compiler facade, representation relocation, or retained intentional cross-module contract.
   - IR concrete implementations (33 types: 13 expressions + 20 statements = 33 types): target M22 (`NON_BLOCKING_P2_FOR_1_0`). Candidate remedies: same as AST concrete nodes.
   - Semantics internal types (11 types: 10 VType/Nullability types + 1 ForeachMetadata = 11 types, totaling 85 PBCIA types): target M22 (`NON_BLOCKING_P2_FOR_1_0`).
   - Remaining interpreter non-prod contracts (`EvaluationValue`, `ExecutionContext`, `EngineInterpreterBridge`, 3 types): classified `INTERNAL_CROSS_MODULE`, target POST_1_0_MAINTENANCE benchmark relocation.
   - **1.0 Blocker Disposition**: All 85 PBCIA types (41 AST / 33 IR / 11 Semantics) are formally reconciled as `NON_BLOCKING_P2_FOR_1_0`. All 85 types have 0 signature leaks into `STABLE_API` or `STABLE_SPI`, cannot be reached by public consumers, `ForeachMetadata` is specified in the 1.0 candidate generated ABI baseline (state: READY_TO_FREEZE), and structural module realignment is deferred to M22.

---

## 4. Remediation Strategy for 1.0 GA (Milestone 0.3.0)

To resolve the 1.0 surface blocker without breaking plugin compilation or runtime bytecode dispatch, Viet Template executes an encapsulation and convergence plan:

### Step 1: Package-Private Visibility Consolidation (0.3.0-M1 — COMPLETE)
Demoted 34 validated intra-package helpers and internal utilities to package-private visibility in sub-milestone `0.3.0-M1`. Accidental public surface reduced to 225 types.

### Step 2: Package Relocation to `*.internal.*` (0.3.0-M2 — COMPLETE: 49 Types Relocated)
Relocated 49 intra-module internal types to subpackages containing `.internal.` across `viet-template-language-vtl` and `viet-template-vtl-interpreter`. Reclassified 17 types to Category C. Migration reference published in `docs/migration/0.3.0-internal-package-moves.md`.

### Step 3: Public Surface Role Classification & Entrypoint Taxonomy (0.3.0-M3.3 — COMPLETE)
Established nine-category taxonomy (ADR-0017). Formally classified 56 cross-module internal production contracts as `INTERNAL_CROSS_MODULE`, 4 framework entrypoints, 4 build-tool entrypoints, and 1 generated ABI type. Contained `ModelSchema$Builder` to package-private. Zero signature leaks.

### Step 4: vtl-interpreter Contract Formalization (0.3.0-M3.4 — COMPLETE)
Formalized 6 interpreter contract types and added `ExecutionTier` API.

### Step 5: Final Architecture Convergence & Debt Registry (0.3.0-M3.5 — COMPLETE)
Classified 55 `INTERNAL_CROSS_PACKAGE` types, 3 `BENCHMARK_SUPPORT_INTERNAL` types, established consumer scope annotations (`MAIN`/`TEST`/`BENCHMARK`) on all 62 contracts, created `public-surface-debt-registry.json` tracking all 85 remaining PBCIA debt types across 3 groups (41 AST concrete nodes, 33 IR concrete implementations [13 expressions + 20 statements = 33 types], and 11 Semantics internal types [10 VType/Nullability types + 1 ForeachMetadata = 11 types], totaling 85 types), clarified JPMS vs package consolidation, and approved `BEGIN_RUNTIME_ARCHITECTURE`. Under M7.9.4 (Public-Internal Debt Reconciliation), all 85 PBCIA types have been reconciled with disposition `NON_BLOCKING_P2_FOR_1_0` based on 0 signature leaks into `STABLE_API` or `STABLE_SPI`, zero reachability by public consumers, `ForeachMetadata` being specified in the 1.0 candidate generated ABI baseline (state: READY_TO_FREEZE), and structural module realignment being deferred to M22.

### Step 6: JPMS Qualified Exports Decision Gate (M22 — DEFERRED)
JPMS evaluation is deferred to M22 when the physical module boundary between `language-vtl` and `vtl-interpreter` is re-evaluated. Note that JPMS does not reduce Java `public` visibility on the classpath; package-private demotion and package consolidation provide the primary line of defense.

### Step 7: Artifact Publication Scope & Manifest Reconciliation (M7.9.5 — COMPLETE)
In milestone M7.9.5, the repository established an authoritative machine-readable publication topology matrix in `config/compatibility/publication-topology.json`, resolving historical release discrepancies, 1.0 GA publication scope, and manifest metadata:
1. **Intended 1.0 Publication Scope (12 Production Modules + Parent POM)**:
   - 13 artifacts will be published to Maven Central for 1.0 GA: `viet-template-parent` (POM), `viet-template-api`, `viet-template-runtime`, `viet-template-language-vtl`, `viet-template-vtl-interpreter`, `viet-template-spring`, `viet-template-spring-security`, `viet-template-spring-boot-autoconfigure`, `viet-template-spring-boot-starter`, `viet-template-maven-plugin`, `viet-template-gradle-plugin`, `viet-template-quarkus`, and `viet-template-quarkus-deployment`.
   - 2 internal modules remain strictly non-published (`<maven.deploy.skip>true</maven.deploy.skip>`): `viet-template-tck` and `viet-template-benchmarks`.
2. **Historical Release Reconciliation (0.2.3 PREPARED_HELD & 0.3.0-SNAPSHOT Unpublished)**:
   - Releases `0.1.0` and `0.2.0` published 5 core artifacts; `0.2.1` and `0.2.2` published 11 artifacts.
   - Commit `b8b3a8f` prepared version `0.2.3` across POMs and build scripts, but `0.2.3` was never tagged in Git and never published to Maven Central. Its historical status is reconciled as `PREPARED_HELD`.
   - Quarkus artifacts (`viet-template-quarkus`, `viet-template-quarkus-deployment`) were introduced during 0.3.0 development (`40974a7`) and were never published under 0.2.x (`Never published. First intended release: 0.3.0+ / 1.0 candidate line`).
   - Active development transitioned to `0.3.0-SNAPSHOT`, which is strictly unpublished (`currentSnapshotPublished: false`).
3. **Factual Manifest Audit (`NO_AUTOMATIC_MODULE_NAME_HEADER`)**:
   - A byte-level audit of built production JAR manifests confirms that `Automatic-Module-Name` headers are omitted (`declared: false`, `status: NO_AUTOMATIC_MODULE_NAME_HEADER`).
   - JPMS modularization is explicitly deferred to post-1.0 (M22 module boundary review). All modules operate cleanly on the standard classpath as unnamed modules.
4. **Gradle Plugin Distribution**:
   - The Gradle plugin (`io.github.minh124199.viet-template`) is published exclusively to Maven Central (`io.github.minh124199:viet-template-gradle-plugin`) and is resolved via `mavenCentral()` in Gradle `pluginManagement.repositories`; it is not published to the Gradle Plugin Portal.

---

## 5. Decision Matrix: 0.3.0 Encapsulation & 1.0 Candidate Evaluation

| Consideration | Defer 1.0 Candidate / Continue Pre-1.0 Iteration | Proceed with 1.0 API Freeze & Release Candidate (RC) |
|---|---|---|
| **SemVer Guarantee** | Unnecessary delay: the 121 stable API/SPI types (94 `STABLE_API` + 27 `STABLE_SPI`) are fully baselined across 5 contract files. Following M7.9.4 reconciliation, the remaining 85 PBCIA debt types (41 AST, 33 IR [13 expressions + 20 statements], 11 Semantics [10 VType/Nullability + 1 ForeachMetadata]) have 0 signature leaks into public API/SPI and are formally classified as non-blocking P2 debt deferred to M22. | **Optimal**: commits to SemVer 2.0.0 for the 121 verified stable types while isolating internal debt behind strict architectural invariants. |
| **Encapsulation & ABI Stability** | Internal encapsulation completed through 0.3.0-M1–M3.5 (34 package-private demotions, 49 internal package relocations, entrypoint taxonomy). | Generated runtime ABI is locked to exactly 7 types and 22 methods (0 fields); canonical diagnostics locked to 31 codes. Full machine-readable candidate contract established in `config/compatibility/1.0-candidate-contract.json`. |
| **Risk Profile** | **High**: prolongs release ambiguity and stalls adoption for downstream enterprise users requiring stable 1.x contracts. | **Low**: 0 P0/P1 blockers, 100% TCK pass rate (80/80), all 15 audit dimensions complete, and automated candidate aggregator verifier (`scripts/verify-1.0-readiness.py`) passing cleanly. |
| **Recommendation** | **REJECTED**: Postponement no longer justified following completion of 0.3.0 internal encapsulation and M7.9 contract evidence reconciliation. | **APPROVED**: Proceed with 1.0 API freeze and Release Candidate (RC) preparation. |

---

## 6. Conclusion & Release Recommendation

Viet Template has successfully achieved functional, architectural, performance, and documentation maturity across all fifteen evaluation dimensions. With internal surface encapsulation (0.3.0-M1 through 0.3.0-M3.5) and 1.0 contract evidence reconciliation (0.3.0-M7.9.1 through M7.9.6) complete, there are **0 P0/P1 blockers** remaining.

All 121 stable API/SPI types are protected by layered baselines, the generated runtime ABI candidate baseline is established at 7 types and 22 methods, the diagnostic codes candidate baseline is established at 31 canonical codes (state: READY_TO_FREEZE), and the 85 non-blocking P2 PBCIA debt types are registered with zero signature leaks.

**Authoritative Recommendation**:
Proceed immediately with the **1.0 API freeze** and begin **1.0.0-RC1 (Release Candidate) preparation and handoff**.
